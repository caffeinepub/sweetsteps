{
  "frontend/src/App.tsx": "Main application component that sets up TanStack Router with a root layout and eight child routes including the new /daily and /fridge routes.",
  "frontend/src/pages/Landing.tsx": "Landing page component that uses TanStack Router Link components for client-side navigation to /signup, /login, /daily, and /fridge routes, with a modal carousel explaining the app's features and two temporary test buttons for accessing the daily and fridge pages without authentication.",
  "frontend/src/index.css": "Single source of truth for global styles, defining chocolate-inspired OKLCH color tokens (caramel primary, warm cream text, deep chocolate backgrounds) and global typography rules that apply Cantata One to all headings and Source Sans 3 to body text.",
  "frontend/src/pages/Signup.tsx": "Signup page with hardened Internet Identity authentication that calls login() synchronously on click (preserving user gesture for Chrome popup), clears stale sessions on mount via effect, validates sessions before redirecting to onboarding, and maintains stable button state through explicit attempt phases.",
  "frontend/src/pages/Login.tsx": "Login page with hardened Internet Identity authentication that calls login() synchronously on click (preserving user gesture for Chrome popup), clears stale sessions on mount via effect, validates sessions before redirecting, and maintains stable button state through explicit attempt phases.",
  "frontend/src/components/RootLayout.tsx": "Root layout component for TanStack Router that wraps the Outlet with OnboardingResultProvider to enable shared onboarding state across routes.",
  "frontend/public/404.html": "SPA fallback page that redirects to index.html when IC asset hosting returns a 404, ensuring deep links to /signup and /login boot the SPA correctly.",
  "frontend/index.html": "HTML entry point with Google Fonts preconnect, stylesheet loading for Cantata One and Source Sans 3, and a startup script that configures process.env.II_URL using Vite HTML env replacement with a safe fallback to https://identity.ic0.app and console warning when VITE_II_URL is missing.",
  "frontend/tailwind.config.js": "Tailwind configuration extended with custom font families (Source Sans 3 for sans/body, Cantata One for headings) and OKLCH color token mappings for the chocolate dark theme.",
  "frontend/src/pages/Onboarding.tsx": "Four-step onboarding flow that checks access on mount and redirects to /weekly-mountain if onboarding is already complete, otherwise collects user inputs and displays Sweet Summit carousel before navigating to Weekly Mountain.",
  "frontend/src/pages/SweetSummit.tsx": "Sweet Summit review screen that displays AI-generated onboarding results including bigGoal, weeklyMountain (name, weeklyTarget, note), and dailyStep as a single item with development-friendly empty state handling.",
  "frontend/src/contexts/OnboardingResultContext.tsx": "React context provider that stores onboarding answers and AI response (with weeklyMountain and dailyStep schema) in-memory for sharing between Onboarding and Sweet Summit pages.",
  "frontend/src/config/ai.ts": "Configuration reader that retrieves AI endpoint URL (defaulting to Groq Chat Completions endpoint) and API key from Vite environment variables, reporting whether the AI integration is properly configured based on API key presence.",
  "frontend/src/lib/aiClient.ts": "AI client that sends Groq Chat Completions requests with strict JSON-only system prompt, validates the response structure for bigGoal/weeklyMountain/dailyStep schema, and surfaces user-friendly errors without logging sensitive configuration.",
  "frontend/src/lib/onboardingPlaceholder.ts": "Placeholder data generator that creates contextual onboarding plan data (bigGoal, sampleWeeklyMountain, sampleDailyStep) locally without network calls, using user inputs to generate personalized sample content.",
  "frontend/src/pages/WeeklyMountain.tsx": "Weekly Mountain page displaying mountain header with icon, progress section with percentage/bar/stats, navigation buttons to /daily and /fridge (with aria-label for explicit navigation intent), and coach's note card using placeholder data.",
  "frontend/src/pages/Daily.tsx": "Daily SweetSteps page that displays placeholder steps using the new JSON shape `{ steps: [{ task: string; time: number }] }` with six random sample tasks rendered in a list showing task text and time duration. Header and footer remain unchanged from the original layout.",
  "frontend/src/pages/Fridge.tsx": "Chocolate Fridge rewards dashboard with viewport-height constrained layout, horizontal inventory cards (flex-row with reduced padding), three period toggle buttons with reduced padding (px-4, size=\"default\", gap-2) to prevent clipping on small viewports, and \"Your Chocolate Fridge\" header without icon.",
  "frontend/src/hooks/useAuthAttemptGuard.ts": "Hook that manages user-initiated auth attempt lifecycle with immediate UI feedback flag, re-entrancy guard, and attempt timestamp tracking.",
  "frontend/src/hooks/useStalledAuthDetection.ts": "Hook that detects when an auth attempt has stalled (e.g., popup blocked) by starting a 3-second timeout and exposing a boolean to show help panel if auth hasn't reached a terminal state.",
  "frontend/src/components/auth/AuthPopupHelpPanel.tsx": "Reusable inline help panel that explains Internet Identity opens a new window, includes 'Allow popups' guidance, provides a Retry action, and renders a visible link to the configured Internet Identity URL.",
  "frontend/src/components/auth/AuthErrorPanel.tsx": "Reusable error panel component that shows user-friendly error messages with Retry and Return to Landing actions for onboarding check failures and unexpected exceptions.",
  "frontend/src/components/auth/AuthDiagnosticsPanel.tsx": "Minimal diagnostics component that displays resolved Internet Identity provider URL and current auth flags (loginStatus, isLoggingIn, isInitializing, identity present) when enabled via ?debugAuth=1.",
  "frontend/src/utils/identityValidation.ts": "Utility module that validates Internet Identity sessions by checking if an identity is non-anonymous and has a valid delegation, and determines if a session is stale and requires fresh login.",
  "frontend/src/components/TaskModal.tsx": "Self-contained modal component that displays task details, manages an adjustable countdown timer (5-60 minutes) with start/pause/resume/cancel controls using setInterval, shows completion confirmation when timer ends, and resets all internal state when closed or cancelled."
}